pipeline {
    agent {
        any { image 'node:7-alpine' }
    }
    stages {
        stage('Test') {
            steps {
                sh '/usr/local/bin/node --version'
            }
        }
    }
}
