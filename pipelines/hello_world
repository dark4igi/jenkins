pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        echo 'Hello World using Jenkins pipelines from git'
        sh 'ls -la'
        
      }
    }
  }
  post {
    always { cleanWs() }
  }
}
